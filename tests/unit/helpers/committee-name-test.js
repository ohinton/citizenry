import { committeeName } from 'citizenry/helpers/committee-name';
import { module, test } from 'qunit';

module('Unit | Helper | committee name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = committeeName([42]);
  assert.ok(result);
});
